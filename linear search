  #include<stdio.h>
int main()
{
	int size[20],i,n,search;
	printf("Enter th size of array :");
	scanf("%d",&n);
	printf("Enter the elements:");
	for(i=0;i<n;i++)
	{
		scanf("%d",&search[i]);
	}
	printf("the array is:");
	for(i=0;i<n;i++)
	{
		printf(" %d",a[i]);
	}
	printf("\nEnter the num to search\n");
	scanf("%d",&search);
	for(i=0;i<n;i++)
	{
		if(a[i]==search)
		{
			printf("Position is: %d",i+1);
		}
	}
}
